<script src="/js/lib/jquery.js"></script>
<script type="text/javascript">
    function getLocationConstant() {
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(onGeoSuccess, onGeoError);
        } else {
            alert("Your browser or device doesn't support Geolocation");
        }
    }

    // If we have a successful location update
    function onGeoSuccess(event) {
        document.getElementById("Latitude").value = event.coords.latitude;
        document.getElementById("Longitude").value = event.coords.longitude;

    }

    // If something has gone wrong with the geolocation request
    function onGeoError(event) {
        alert("Error code " + event.code + ". " + event.message);
    }
    window.onload = getLocationConstant();
</script>
<div class="container">
  <div class="panel">
    <div class="panel-body">
      {{#if messages.success}}
        <div role="alert" class="alert alert-success">
          {{#each messages.success}}
            <div>{{msg}}</div>
          {{/each}}
        </div>
      {{/if}}
      {{#if messages.error}}
        <div role="alert" class="alert alert-danger">
          {{#each messages.error}}
            <div>{{msg}}</div>
          {{/each}}
        </div>
      {{/if}}
      <form method="POST" action="/account?_method=PUT" class="form-horizontal">
        <legend>Profile Information</legend>
        <div class="form-group">
          <label for="email" class="col-sm-3">Email</label>
          <div class="col-sm-7">
            <input type="email" name="email" id="email" class="form-control" value="{{user.email}}">
          </div>
        </div>
        <div class="form-group">
          <label for="name" class="col-sm-3">Name</label>
          <div class="col-sm-7">
            <input type="text" name="name" id="name" class="form-control" value="{{user.name}}">
          </div>
        </div>
        <div class="form-group">
          <label class="col-sm-3">Gender</label>
          <div class="col-sm-4">
            <label class="radio-inline radio col-sm-4">
              <input type="radio" name="gender" value="male" {{#ifeq user.gender 'male'}}checked{{/ifeq}}><span>Male</span>
            </label>
            <label class="radio-inline col-sm-4">
              <input type="radio" name="gender" value="female" {{#ifeq user.gender 'female'}}checked{{/ifeq}}><span>Female</span>
            </label>
          </div>
        </div>
        <div class="form-group">
          <label for="location" class="col-sm-3">Location</label>
          <div class="col-sm-7">
            <input type="text" name="location" id="location" class="form-control" value="{{user.location}}">
          </div>
        </div>
        <div class="form-group">
          <label for="website" class="col-sm-3">Website</label>
          <div class="col-sm-7">
            <input type="text" name="website" id="website" class="form-control" value="{{user.website}}">
          </div>
        </div>
        <div class="form-group">
          <label class="col-sm-3">Gravatar</label>
          <div class="col-sm-4">
            <img src="{{user.picture}}" class="profile" >
          </div>
        </div>

        </br>
          <legend>Display Information</legend>



          <input type="hidden" id="Latitude" name="Latitude" value="">
          <br>

          <input type="hidden" id="Longitude" name="Longitude" value="">


          <div class="form-group">
              <label for="name" class="col-sm-3">Display Name</label>
              <div class="col-sm-7">
                  <input type="text" name="displayname" id="displayname" class="form-control" value="{{user.displayname}}">
              </div>
          </div>


          <div class="form-group">
              <label for="location" class="col-sm-3">Age</label>
              <div class="col-sm-7">
                  <input type="text" name="age" id="age" class="form-control" value="{{user.age}}">
              </div>
          </div>

          <div class="form-group">
              <label for="location" class="col-sm-3">About Me</label>
              <div class="col-sm-7">
                  <input type="text" name="aboutme" placeholder="define urself in 30 words" id="aboutme" class="form-control" value="{{user.aboutme}}">
              </div>
          </div>
          <div class="form-group">
              <label for="website" class="col-sm-3">Work At</label>
              <div class="col-sm-7">
                  <input type="text" name="workat" id="workat" class="form-control" value="{{user.workat}}">
              </div>
          </div>

          <div class="form-group">
              <label for="website" class="col-sm-3">Studying At</label>
              <div class="col-sm-7">
                  <input type="text" name="studying" id="studying" class="form-control" value="{{user.studying}}">
              </div>
          </div>
          <div class="form-group">
              <label for="website" class="col-sm-3" >Interest</label>

              <label class="radio-inline col-sm-4">
                      <select id="ddlCars3"  multiple="multiple" name="interest">
                          <option value="Startup">Startup</option>
                          <option value="Coding">Coding</option>
                          <option value="Photography">Photography</option>
                          <option value="Music">Music</option>
                          <option value="Movies">Movies</option>
                          <option value="Gaming">Gaming</option>
                          <option value="Food">Food</option>
                      </select>
                  <p> {{user.interest}}</p>
              </label>
          </div>
        <div class="form-group">
          <div class="col-sm-offset-3 col-sm-4">
            <button type="submit" class="btn btn-success">Update Profile</button>
          </div>
        </div>
      </form>
    </div>
  </div>
  <div class="panel">
    <div class="panel-body">
      <form method="POST" action="/account?_method=PUT" class="form-horizontal">
        <legend>Change Password</legend>
        <div class="form-group">
          <label for="password" class="col-sm-3">New Password</label>
          <div class="col-sm-7">
            <input type="password" name="password" id="password" class="form-control">
          </div>
        </div>
        <div class="form-group">
          <label for="confirm" class="col-sm-3">Confirm Password</label>
          <div class="col-sm-7">
            <input type="password" name="confirm" id="confirm" class="form-control">
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-4 col-sm-offset-3">
            <button type="submit" class="btn btn-success">Change Password</button>
          </div>
        </div>
      </form>
    </div>
  </div>
  <div class="panel">
    <div class="panel-body">
      <div class="form-horizontal">
        <legend>Linked Accounts</legend>
        <div class="form-group">
          <div class="col-sm-offset-3 col-sm-4">
          {{#if user.facebook}}
            <p><a href="/unlink/facebook" class="text-danger">Unlink your Facebook account</a></p>
          {{else}}
            <p><a href="/auth/facebook">Link your Facebook account</a></p>
          {{/if}}
          {{#if user.twitter}}
            <p><a href="/unlink/twitter" class="text-danger">Unlink your Twitter account</a></p>
          {{else}}
            <p><a href="/auth/twitter">Link your Twitter account</a></p>
          {{/if}}
          {{#if user.google}}
            <p><a href="/unlink/google" class="text-danger">Unlink your Google account</a></p>
          {{else}}
            <p><a href="/auth/google">Link your Google account</a></p>
          {{/if}}
          {{#if user.vk}}
            <p><a href="/unlink/vk" class="text-danger">Unlink your VK account</a></p>
          {{else}}
            <p><a href="/auth/vk">Link your VK account</a></p>
          {{/if}}
          {{#if user.github}}
            <p><a href="/unlink/github" class="text-danger">Unlink your Github account</a></p>
          {{else}}
            <p><a href="/auth/github">Link your Github account</a></p>
          {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="panel">
    <div class="panel-body">
      <form method="POST" action="/account?_method=DELETE" class="form-horizontal">
        <legend>Delete Account</legend>
        <div class="form-group">
          <p class="col-sm-offset-3 col-sm-9">You can delete your account, but keep in mind this action is irreversible.</p>
          <div class="col-sm-offset-3 col-sm-9">
            <button type="submit"  class="btn btn-danger">Delete my account</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>



<script type="text/javascript">
    $(document).ready(function() {
        $('#ddlCars3').multiselect({
            nonSelectedText :'Select more than one'

        });
    });
</script>

